%% include 'header.rs.jinja'

#[derive(Debug, PartialEq, Eq, Copy, Clone, Default, Hash, Ord, PartialOrd)]
pub enum Currency {
    #[default] Free,
%% for ptype in price_types
    {{ ptype|capitalize }}(i32),
%% endfor
}

impl analyzer::world::Id for Currency {}
